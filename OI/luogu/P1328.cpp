#include <iostream>
using namespace std;
int main()
{
    int n, T_A, T_B;
    int A[202], B[202];
    int A_score = 0, B_score = 0;
    int p = 1, q = 1;
    cin >> n >> T_A >> T_B;
    for (int i = 1; i <= T_A; i++)
        cin >> A[i];
    for (int i = 1; i <= T_B; i++)
        cin >> B[i];

    while (n)
    {
        n--;
        switch(A[p]){
            case 1:
        }
    }
    return 0;
}